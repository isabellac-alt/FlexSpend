[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for client routes (static assets are still served from /dist first)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
